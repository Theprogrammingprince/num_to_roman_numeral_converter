# Welcome to My Roman Numerals Converter
Welcome To My Roman NUmerals Converter 

## Task
it takes simple number value and returns the 
roman numeral equivalent
## Description
Created a function that accepts a single num value
that returns a string
inside my function i said if param <= 0 or greater than 3999
return an empty string
then i created a list with number and and roman symbol
then i created an empty dtring to store our values
then i went through my list 
to check if the param matches if it does the while loop ends if doesn't it continues until it does
and then returns the value
## Installation
does not require installation

## Usage
clone repo
ruby name of file 
inside the file call the function puts (name of function(and value))
```

### The Core Team
Paul Prince